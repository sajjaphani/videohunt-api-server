// Supplies the API URLs used by the API server
var path = require('path');

function urls() {

     /**
     * Exposed environment.
     * 
     * @public
     */
    var environment = {
        
    }

    return environment;
}

module.exports = urls()